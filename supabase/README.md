# Supabase Project

This Supabase project is dedicated to the offline marketing file graph.

Primary migration:

- `migrations/20260617000000_offline_marketing_files.sql`

Use `supabase link --project-ref <project-ref>` and `supabase db push` after choosing the remote Supabase project.
